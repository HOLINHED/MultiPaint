{
  "name": "multilpaint",
  "version": "1.0.0",
  "description": "paint program that gets paint data from all connected clients and displays in a single window.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "Dawid Klatkiewicz",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3",
    "socket.io": "^2.1.1"
  },"engines": {
    "node": "8.11.1"
  }
}
